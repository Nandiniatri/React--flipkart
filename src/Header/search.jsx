import Input from "./input";

export default function Search(){
    return (
        <div>
            <Input className={'search'} type={'text'} placeholder={'Searching For The Product'}/>
        </div>
    )
}